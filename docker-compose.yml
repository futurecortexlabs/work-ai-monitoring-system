version: '3.9'

services:

  ai-service:
    build: .
    container_name: work-ai-service
    restart: always

    volumes:
      - ./:/app

    ports:
      - "8000:8000"

    environment:
      - PYTHONUNBUFFERED=1

    command: python src/main.py